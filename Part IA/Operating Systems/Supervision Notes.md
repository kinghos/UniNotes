Memory Management Unit - CPU hardware that manages base/limits on memory usage for a process
SJF has the issue of relying on heuristics
Lower priority (integer wise) is higher
Round robin can be seen as having dynamic priority
I/O events are prioritised because they have shorter CPU bursts
interleaving can alleviate this to ensure cpu-bound jobs get time

Program is the executable binary
Processes are the binary being executed
Threads are in processes